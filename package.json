{
  "name": "typescript-data-structures-and-algorithms",
  "version": "1.0.0",
  "description": "Custom implementations of Data Structures and Algorithms in TypeScript",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "tsx": "^4.23.0",
    "typescript": "^5.9.2"
  }
}
